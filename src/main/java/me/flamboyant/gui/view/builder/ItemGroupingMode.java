package me.flamboyant.gui.view.builder;

public enum ItemGroupingMode {
    // No visible separation
    PACKED,
    // Separated by an empty space
    PARTED,
    // each group has its own line
    EXCLUSIVE_LINE,
    // each group has its own page
    EXCLUSIVE_PAGE
}
